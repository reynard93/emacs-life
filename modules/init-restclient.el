(use-package restclient
  :pin melpa
  :defer t
  :config
  (message "restclient is loaded"))

(use-package ob-restclient
  :pin melpa
  :defer t
  :config
  (message "ob-restclient is loaded"))

(provide 'init-restclient)
